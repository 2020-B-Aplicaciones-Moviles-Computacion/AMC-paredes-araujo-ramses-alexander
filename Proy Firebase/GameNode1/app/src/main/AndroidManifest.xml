<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gamenode1">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/controller3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/controller3"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameNode1">
        <activity android:name=".ActividadPerfilGrupo"></activity>
        <activity android:name=".ActividadPerfilInvitado" />
        <activity android:name=".ActividadEditarUsuario" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".ActividadMapa"
            android:label="@string/title_activity_actividad_mapa" />
        <activity android:name=".ActividadSeleccionarUbicacion" />
        <activity android:name=".ActividadAgregarComentarioPublicacion" />
        <activity android:name=".ActividadCrearGrupo" />
        <activity android:name=".ActividadAgregarNuevaPublicacion" />
        <activity android:name=".ActividadAgregarJuegoBiblioteca" />
        <activity android:name=".ActividadCrearJuego" />
        <activity android:name=".ActividadMenuPrincipal" />
        <activity android:name=".ActividadCrearUsuario" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>